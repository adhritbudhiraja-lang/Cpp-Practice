#include <iostream>
#include <vector>

int main()
{
    std::vector<int> num; // std::vector <data_type> variable;
    num.push_back(10); // num = [10];
    num.push_back(20); // num = [10,20];
    return 0;
    // std::vector<int> num = {10,20,30};  // Also Works
}